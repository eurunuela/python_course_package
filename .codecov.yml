codecov:
  notify:
    require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        # basic
        target: 0
        threshold: 0
    patch:
      default:
        target: 0
        threshold: 0

ignore:
  - "workout_analysis/tests/"
  - "workout_analysis/due.py"
  - "workout_analysis/_version.py"
  - "workout_analysis/info.py"
